{
  "name": "ritvik-academic-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:graph": "node scripts/build-graph.mjs",
    "dev": "python3 -m http.server 5173",
    "build": "npm run build:graph"
  }
}
